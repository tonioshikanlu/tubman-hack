package l.b.c;

import b.e.a.a.a;
import l.b.c.c;

public abstract class l {
    public static final l a = new c(false, (r) null, (c.a) null);

    static {
        String str = Boolean.FALSE == null ? " sampleToLocalSpanStore" : "";
        if (str.isEmpty()) {
            return;
        }
        throw new IllegalStateException(a.m("Missing required properties:", str));
    }

    public abstract boolean a();

    public abstract r b();
}
